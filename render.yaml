services:
  - type: web
    name: Telegram-YT-BOT
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: COOKIES
        sync: false
